@startuml EgressPolicyDecideSequence
' SPEC source: internal/egresspolicy/policy.go
!includeurl https://raw.githubusercontent.com/proveo-ca/identity/refs/heads/main/proveo.iuml
skinparam shadowing false

title Egress Policy — ""Decide(req)"" sequence (off-provider)

participant "Agent" as Agent
participant "brokerModifier" as Broker
participant "policyModifier\n/ Decide" as Policy
participant "Origin" as Origin
database "flows.ndjson" as Log

== Provider host (pinned) ==
Agent -> Broker : HTTPS request
Broker -> Broker : inject auth header
Broker -> Origin : forward (policy no-op)
note right of Policy
Policy skips provider hosts so a
credential bound for the vendor is
never flagged by DLP.
end note

note over Broker, Policy
  **The exemption is not the broker's to give.** It reads "broker-injected
  credential" naturally enough, but the set of provider hosts is a
  //separate input// to the policy, and it has to be — under
  ""--credentials forward"" the broker is inert by design: the agent holds
  the real key and calls the vendor itself, so there is no route to
  derive the set from.

  Deriving it from ""broker\.Config\.Routes"" alone left it **empty** in
  exactly that posture, and the scan then matched the live key in
  ""x-api-key"" against the generic ""sk-"" shape and answered ""403
  egress policy: blocked (secret)"". The one destination the credential
  exists to reach was the only one denied, and the agent reported it as
  an auth failure — so the operator read "bad key", not "policy".

  ""PROVEO_EGRESS_PROVIDER_HOSTS"" states the set independently: routes
  when there are routes, plus the endpoints ""proveo run"" resolved for
  the run. It stays **per-destination** rather than becoming a DLP
  off-switch — the same key leaving for anywhere else is still blocked.
end note

== Off-provider — allow path ==
Agent -> Broker : e.g. GET https://docs.example.com
Broker -> Broker : strip credential headers
Broker -> Policy : Decide
Policy -> Policy : B: not a sink?
Policy -> Policy : A: method is read\n(GET/HEAD/OPTIONS)?
Policy -> Policy : C: no secret / under budget?
Policy --> Log : allowed (optional)
Policy -> Origin : upstream round-trip

== Off-provider — block paths ==
Agent -> Broker : POST / secret in query / sink host
Broker -> Broker : strip credential headers
Broker -> Policy : Decide
alt reason = sink (B)
  Policy --> Log : blocked, reason=sink
else reason = write-not-allowlisted (A)
  Policy --> Log : blocked, reason=write
else reason = secret | budget (C)
  Policy --> Log : blocked, reason=secret|budget\n(never log secret value)
end
Policy -[#6E1100,bold]-> Agent : 403 + SkipRoundTrip\n(origin never contacted)

note over Policy
**Data-diode bias:** broad reads, tight writes.
Not a hermetic seal — slow drip / covert channels remain;
goal is to make bulk exfil hard and auditable.
end note

@enduml
